spring:
  datasource:
    url: jdbc:h2:mem:rapidtox_data;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=create schema if not exists rapidtox\;create schema if not exists rapidtox_app_data
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate.ddl-auto: update
